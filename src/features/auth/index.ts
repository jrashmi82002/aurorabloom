// Auth feature barrel — page, hooks, services in one import surface.
export { default as AuthPage } from "@/pages/Auth";
export { useAuth } from "@/hooks/useAuth";
export { authService } from "@/services/auth.service";
